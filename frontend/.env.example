# Frontend env example.
# Vite は VITE_ プレフィックスのみクライアントに露出。

VITE_API_BASE_URL=http://localhost:8080

# Phase 1 はシングルユーザー前提。
# APIキーは初回起動時にバックエンドからstdout出力されるものを設定。
VITE_API_KEY=orb_xxxxxxxxxxxxxxxxxxxxxxxxxxxx
